# 劳动法 RAG 系统 — 私有化部署方案

---

## 一、为什么法律 AI 必须私有化部署

### 1.1 当前你的架构（公有云模式）

```
用户输入"我在XX公司被违法辞退，工号007，月薪2万"
         │
         v
    Spring Boot ──→ FastAPI ──→ DeepSeek API (公有云)
                                      │
                              你的案件详情、个人信息
                              全部明文发给第三方服务器
```

**问题**：每一步都是泄密风险。

### 1.2 法律行业的三条红线

| 红线 | 来源 | 含义 |
|------|------|------|
| **律师-客户特权** | 《律师法》第38条 | 律师不得泄露委托人的商业秘密和个人隐私 |
| **个人信息保护法** | 《个人信息保护法》第13条 | 处理敏感个人信息需取得单独同意 |
| **数据安全法** | 《数据安全法》第21条 | 重要数据出境需安全评估 |

**结论**：律所如果把你系统接到公网 API，等于把客户案件材料发给 DeepSeek/OpenAI 的服务器。这在法律上是**违规**的，在商业上是**不可接受**的。

### 1.3 真实场景

> 金杜律师事务所（中国最大律所之一）使用 AI 工具时，要求：
> - 所有数据不出所内网络
> - LLM 推理在本地服务器完成
> - 外网 API 仅限非敏感的法规查询
> - 完整审计日志保留 5 年

---

## 二、私有化部署的三种方案

### 方案 A：纯本地（最安全，成本最高）⭐⭐⭐

```
┌─────────────────────────────────────────────┐
│              律所内网 / 企业服务器              │
│                                               │
│  Vue前端 → SpringBoot → FastAPI → 本地LLM     │
│                  │              │             │
│              MySQL/Neo4j    Ollama/vLLM       │
│              Redis/FAISS    (GPU服务器)        │
│                                               │
│  所有数据不出内网，LLM 推理在本地 GPU 完成       │
└─────────────────────────────────────────────┘
```

**需要的硬件**：

| 模型 | 参数量 | 显存需求 | 推理速度 | 法律能力 |
|------|--------|---------|---------|---------|
| Qwen2.5-7B | 7B | ~16GB | 快 | 中文好，但法律弱 |
| Qwen2.5-14B | 14B | ~28GB | 中等 | 中文好，法律尚可 |
| Qwen2.5-32B | 32B | ~64GB | 较慢 | 可以胜任法律任务 |
| Qwen2.5-72B | 72B | ~144GB(多卡) | 慢 | 接近 GPT-4 水平 |
| DeepSeek-V3 | 671B(MoE) | ~40GB(激活) | 中等 | 你的 API 同款 |

**推荐配置**：
- **入门**：RTX 4090(24GB) + Qwen2.5-14B 量化版 → ¥2-3万
- **可用**：RTX A6000(48GB) + Qwen2.5-32B 量化版 → ¥5-8万
- **专业**：A100(80GB) + Qwen2.5-72B → ¥15-20万

**优点**：完全合规，无需网络，速度可控
**缺点**：需要 GPU 硬件投入，模型能力不如云端最新版

### 方案 B：私有化 API 代理（折中方案）⭐⭐

```
┌────────── 律所内网 ──────────┐    ┌── 云平台 VPC ──┐
│                              │    │                 │
│  Vue → SpringBoot → FastAPI ─┼────→ 私有化 LLM API │
│       │            │         │    │  (专线/VPN)     │
│   MySQL/Neo4j   FAISS/BM25   │    │  数据不落盘     │
│                              │    │  用完即焚       │
└──────────────────────────────┘    └─────────────────┘
```

**工作方式**：律所部署业务层（后端+数据库+检索），LLM 推理通过专线/VPN 调用云上专属实例。数据和 LLM 之间通过加密通道传输，云平台不存储数据。

**优点**：无需买 GPU，模型可以用最强的
**缺点**：仍有数据离开内网的传输环节，需要签 DPA（数据处理协议）

### 方案 C：混合架构（你当前最容易实现的）⭐

```
┌──── 本地(免费) ────┐     ┌── 需要时可切 ──┐
│                     │     │                │
│  嵌入模型(本地CPU)   │     │  DeepSeek API  │
│  FAISS/BM25(本地)   │     │  或本地 LLM    │
│  Neo4j(本地)        │     │                │
│  MySQL(本地)        │     │                │
│                     │     │                │
│  已合规：原始法条    │     │  需处理：      │
│  不出内网           │     │  用户问题含隐私  │
└─────────────────────┘     └────────────────┘
```

**关键洞察**：你的系统中，传给 LLM 的是**检索到的法条内容 + 用户问题**。法条本身不涉密，但**用户的问题描述**（如"我在XX公司被辞退"）包含隐私。

**混合策略**：默认用本地小模型回答（不联网），只有纯法条查询类问题（如"劳动合同法第47条是什么"）才走云端大模型。

---

## 三、针对你的系统的具体实施方案

### 第一步：接入本地 LLM（Ollama）

替换 `chain.py` 中的 `ChatOpenAI` 为本地模型，改动量极小：

```python
# 当前代码 (chain.py 第53行)
self.llm = ChatOpenAI(
    model="deepseek-v4-pro",
    api_key=Config.LLM_API_KEY,
    base_url="https://api.deepseek.com/v1",
)

# 改为本地模型
self.llm = ChatOpenAI(
    model="qwen2.5:14b",        # Ollama 中的模型名
    api_key="not-needed",
    base_url="http://localhost:11434/v1",  # Ollama 默认地址
)
```

Ollama 安装（一行命令）：
```bash
# Linux/macOS
curl -fsSL https://ollama.com/install.sh | sh

# 拉取中文法律能力较好的模型
ollama pull qwen2.5:14b      # 14B 参数，需要 ~16GB 内存/显存
ollama pull qwen2.5:32b      # 32B 参数，需要 ~32GB 内存/显存(推荐)
```

### 第二步：Prompt 适配

本地模型比 DeepSeek 弱，需要更强的 Prompt 约束：

```python
# 当前 System Prompt 基础上，增加更严格的约束
LOCAL_SYSTEM_PROMPT = SYSTEM_PROMPT + """

## 额外约束（本地模型专用）
7. **严格引用**：每句话如果引用了法条，必须在句尾标注出处
8. **禁止自由发挥**：如果检索资料中没有相关内容，只说"未找到相关依据"
9. **控制长度**：每个回答不超过 500 字，分点列明
"""
```

### 第三步：配置文件切换

在 `.env` 中加一个开关：

```ini
# 私有化部署开关
DEPLOY_MODE=local           # local = 本地LLM, cloud = DeepSeek API
LOCAL_LLM_URL=http://localhost:11434/v1
LOCAL_LLM_MODEL=qwen2.5:14b
```

`config.py` 读取并切换：

```python
class Config:
    DEPLOY_MODE: str = os.getenv("DEPLOY_MODE", "cloud")
    LOCAL_LLM_URL: str = os.getenv("LOCAL_LLM_URL", "http://localhost:11434/v1")
    LOCAL_LLM_MODEL: str = os.getenv("LOCAL_LLM_MODEL", "qwen2.5:14b")
```

`chain.py` 初始化时判断：

```python
if Config.DEPLOY_MODE == "local":
    llm_kwargs["base_url"] = Config.LOCAL_LLM_URL
    llm_kwargs["model"] = Config.LOCAL_LLM_MODEL
```

### 第四步：数据分级

| 数据 | 隐私级别 | 存储位置 | 传输方式 |
|------|---------|---------|---------|
| 法律条文原文 | 公开 | MySQL/Neo4j 本地 | 无传输 |
| 裁判文书（已公开） | 公开 | MySQL/Neo4j 本地 | 无传输 |
| 用户问题 | **隐私** | MySQL 本地加密 | 仅本地 LLM |
| 问答历史 | **隐私** | MySQL 本地加密 | 仅本地 LLM |
| 用户身份信息 | **隐私** | MySQL 本地加密 | 无传输 |
| 嵌入向量 | 公开 | FAISS 本地 | 无传输 |
| KG 实体关系 | 公开 | Neo4j 本地 | 无传输 |

**核心发现**：你系统里 80% 的数据（法条、案例、KG）本身就是公开的，不需要私有化。只有**用户的问题和身份信息**是隐私数据，需要保护。

---

## 四、成本估算

### 4.1 一次性投入

| 项目 | 入门版 | 可用版 | 专业版 |
|------|--------|--------|--------|
| GPU | RTX 4090 24GB ¥1.5万 | A6000 48GB ¥5万 | A100 80GB ¥15万 |
| 服务器 | 台式机 ¥5千 | 塔式工作站 ¥1.5万 | 机架式 ¥3万 |
| 内存 | 32GB ¥1千 | 64GB ¥2千 | 128GB ¥5千 |
| 存储 | 1TB SSD ¥500 | 2TB SSD ¥1千 | 4TB NVMe ¥3千 |
| **合计** | **~¥2.2万** | **~¥6.8万** | **~¥18.8万** |

### 4.2 持续成本（月）

| 项目 | 入门版 | 专业版 |
|------|--------|--------|
| 电费 | ~¥300/月 | ~¥800/月 |
| 运维人力 | 0（自动化） | 兼职运维 ¥3千/月 |
| 模型更新 | 免费（开源） | 免费（开源） |
| DeepSeek API（备用） | ~¥200/月 | ~¥500/月 |

### 4.3 vs 公有云 API 成本对比

| | 公有云(DeepSeek) | 私有化(本地 LLM) |
|------|---------|---------|
| 前期投入 | ¥0 | ¥2-18万 |
| 月均 | ¥200-500 | ¥300-800(电费) |
| 数据合规 | ❌ 不满足 | ✅ 满足 |
| 回答质量 | ⭐⭐⭐⭐ | ⭐⭐⭐（取决于模型） |
| 适用场景 | 个人学习/论文 | **律所/企业** |

---

## 五、实施路线图

```
Week 1-2: Ollama 本地部署 + 模型评测
  - 安装 Ollama，拉取 qwen2.5:14b/32b
  - 用你的 50 道测试题对比 DeepSeek vs 本地模型
  - 选最优模型，记录质量差距

Week 3: 代码适配
  - chain.py 增加本地 LLM 支持
  - config.py 增加 DEPLOY_MODE 开关
  - .env 增加部署配置

Week 4: 安全加固
  - 用户数据加密存储
  - API 请求日志脱敏
  - 数据传输加密(HTTPS + 内网)

Week 5-6: 打包交付
  - Docker 容器化（一键部署）
  - 部署文档（律所 IT 能看懂）
  - 演示环境搭建
```

---

## 六、一句话总结

**私有化部署的核心不是技术，是信任。** 律所不会把客户的案件材料发到任何第三方服务器。解决方法是：把 DeepSeek API 换成跑在本地的开源大模型（如 Qwen2.5），改动不到 10 行代码，但硬件需要一块 GPU（¥2 万起）。
